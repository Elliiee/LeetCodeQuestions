import java.util.LinkedList;
import java.util.Queue;

public class _409TheMaze {
    int[][] directions = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};

    /*public boolean hasPath(int[][] maze, int[] start, int[] destination){
        Queue<int[]> queue = new LinkedList<>();
        queue.add(start);
    }
    */

}
